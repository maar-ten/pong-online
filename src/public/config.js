const GAME_CONFIG = {
    // some general settings
    GAME_WIDTH: 900,
    GAME_HEIGHT: 540,
    FPS_LOGGER: true, // shows/hides the frames per second in the top left corner
    MPS_LOGGER: true, // shows/hides the messages per second in the top left corner
    ROBOT_ENABLED: true,
}

export default GAME_CONFIG;